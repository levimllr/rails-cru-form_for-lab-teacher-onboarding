<h1><%= @artist.name %></h1>
<% if flash[:notice] %>
  <div class="notice"><%= flash[:notice] %></div>
<% end %>
<p><%= @artist.bio %></p>
<ul>
<%= @artist.songs.each do |song| %>
<li><%= song.name + ", " + song.genre %></li>
<% end %>